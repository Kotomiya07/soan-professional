{
  "name": "soan-professional-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:core": "npm --prefix packages/cli exec tsc -- -p packages/core/tsconfig.json",
    "check": "pixi run check"
  }
}
