# @soan-professional/core

Renderer-independent Professional contracts shared by the CLI compatibility renderer and future renderers.

This package intentionally contains pure TypeScript helpers only:

- page layout option normalization
- manual glyph offset validation
- morphology token normalization for MeCab / UniDic adapters

PixiJS editing is outside this package.
